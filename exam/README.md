# Frontend

## Development

```bash
# install Yarn
npm install yarn -g

# check yarn version
yarn -v

# install all dependencies
yarn install

# run in dev mode
yarn dev

# build package
yarn build
```
