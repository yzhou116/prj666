# Frontend

## Development

```bash
# install Yarn
npm install yarn -g

# check yarn version
yarn -v

# use custom npm mirror
# TODO: what is the point of this
# yarn config set registry https://registry.npm.taobao.org

# install all dependencies
yarn install

# run in dev mode
yarn dev

# build package
yarn build
```
